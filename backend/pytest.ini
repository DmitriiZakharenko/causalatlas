[pytest]
markers =
    live_llm: test makes a real `claude` CLI invocation (costs subscription usage + wall-clock time). Deselected by default -- run explicitly with `pytest -m live_llm`.
addopts = -m "not live_llm"
asyncio_mode = auto

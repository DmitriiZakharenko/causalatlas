VITE_API_BASE_URL=http://localhost:8000
# Frontend defaults to offline snapshots for a backend-free first run.
# Set false when the FastAPI backend is running and live API calls are desired.
VITE_OFFLINE_MODE=true
